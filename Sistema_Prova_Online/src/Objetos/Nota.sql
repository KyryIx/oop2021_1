CREATE TABLE `test`.`nota` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `codigo` INT NOT NULL,
  `valor` FLOAT NOT NULL,
  `peso` FLOAT NOT NULL,
   PRIMARY KEY (`id`));

INSERT INTO `nota` (`codigo`, `valor`, `peso`) VALUES (46164, 8.0, 1.0);